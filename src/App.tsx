// ============================================================================
// アプリのルート — 3Dシーン(背景) + HUD(前面)
// ============================================================================
import { TabletGame } from './components/TabletGame';

export default function App() {
  return <TabletGame />;
}
