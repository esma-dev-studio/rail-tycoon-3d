// ============================================================================
// アプリのルート — 3Dシーン(背景) + HUD(前面)
// ============================================================================
import { RailTownApp } from './components/RailTownApp';

export default function App() {
  return <RailTownApp />;
}
