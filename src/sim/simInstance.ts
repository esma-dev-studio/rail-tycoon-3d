// ============================================================================
// シミュレーションの単一インスタンス(React再描画から切り離した可変状態)
// ============================================================================
import { createSim } from './simulation';

export const sim = createSim();
